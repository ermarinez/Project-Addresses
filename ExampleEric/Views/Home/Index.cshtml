<!DOCTYPE html>
<html lang="es">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Página de Inicio</title>
    <style>
        /* Estilo general de la página */
        body {
            font-family: Arial, sans-serif;
            background-color: #f8f9fa; /* Color de fondo claro */
            color: #333;
            display: flex;
            flex-direction: column;
            align-items: center;
            margin: 0;
            padding: 0;
            text-align: center;
            height: 100vh;
        }

        /* Encabezado con navegación */
        header {
            width: 100%;
        }

        /* Estilo del contenedor principal */
        .container {
            display: flex;
            flex-direction: column;
            align-items: center;
            max-width: 600px;
            margin-top: 20px;
            padding: 20px;
            background-color: #ffffff;
            border-radius: 8px;
            box-shadow: 0px 4px 10px rgba(0, 0, 0, 0.1);
            color: #333;
        }

        /* Estilo del título */
        h1 {
            font-size: 2.5em;
            color: #007bff; /* Azul similar a los botones */
            font-weight: bold;
            margin: 0;
        }

        /* Estilo del texto de bienvenida */
        p {
            font-size: 1.2em;
            color: #555;
            margin-top: 15px;
            line-height: 1.6;
        }

        /* Barra de navegación y botones */
        .navbar {
            background-color: #ffffff;
            border-bottom: 1px solid #ddd;
            box-shadow: 0px 2px 8px rgba(0, 0, 0, 0.1);
        }

        .navbar-brand {
            color: #007bff;
            font-weight: bold;
        }

            .navbar-brand:hover {
                color: #0056b3;
                text-decoration: none;
            }

        .nav-link {
            color: #555;
            font-weight: bold;
        }

            .nav-link:hover {
                color: #007bff;
            }
    </style>
</head>
<body>
@*     <header>
        <nav class="navbar navbar-expand-sm navbar-light bg-white border-bottom box-shadow mb-3 custom-navbar">
            <div class="container-fluid">
                <div class="navbar-header">
                    <a class="navbar-brand" asp-area="" asp-controller="Home" asp-action="Index">Inicio</a>
                </div>
                <div class="navbar-collapse collapse d-sm-inline-flex align-items-start">
                    <ul class="navbar-nav custom-nav">
                        <li class="nav-item">
                            <a class="nav-link" href="https://localhost:7235/clients">Clientes</a>
                        </li>
                        <li class="nav-item">
                            <a class="nav-link" href="https://localhost:7235/addresses">Direcciones</a>
                        </li>
                    </ul>
                </div>
            </div>
        </nav>
    </header> *@

    <div class="container">
        <h1>Bienvenido a Nuestra Página</h1>
        <p>Nos alegra mucho que estés aquí. Explora nuestros clientes y direcciones para descubrir todo lo que tenemos para ofrecer.</p>
    </div>
</body>
</html>





